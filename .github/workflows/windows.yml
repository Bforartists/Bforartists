name: Windows

on:
  # push:
  #   branches: [ "work-sequencer" ]
  # schedule:
  #   - cron: 0 0 * * 1
  pull_request:
    branches: [ master ]

permissions: {}
jobs:
  build:

    runs-on: windows-latest

    steps:
    - name: make
      run: |
        mkdir ~\bforartistsrepo
        cd ~\bforartistsrepo
        git clone https://github.com/Bforartists/Bforartists
        mkdir ~\bforartistsrepo\lib
        cd ~\bforartistsrepo\lib
        svn checkout https://svn.blender.org/svnroot/bf-blender/trunk/lib/win64_vc15/
        dir
        cd ~\bforartistsrepo\Bforartists
        .\make.bat lite
        dir
        cd ~\bforartistsrepo
        dir


    - name: Upload Build
      uses: actions/upload-artifact@v2
      with:
        name: Bforartists-Release-${{ runner.os }}
        path: C:/Users/runneradmin/bforartistsrepo/bfa_build_windows_*_x64_vc17*/bin
